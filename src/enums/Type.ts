export enum Role {
	STUDENT = 'student',
	REVIEWER = 'reviewer',
	ADMIN = 'admin'
}

export enum Gender {
	MALE = 'male',
	FEMALE = 'female',
	OTHER = 'other'
}

export enum Difficulty {
	EASY = 'easy',
	MEDIUM = 'medium',
	HARD = 'hard'
}

export enum Status {
	PRIVATE = 'private',
	PUBLIC = 'public'
}

export enum MessageStatus {
	SENT = 'sent',
	RECEIVED = 'received'
}
