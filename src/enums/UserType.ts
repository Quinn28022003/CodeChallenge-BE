export enum Role {
	STUDENT = 'student',
	REVIEWER = 'reviewer',
	ADMIN = 'admin'
}

export enum Gender {
	MALE = 'male',
	FEMALE = 'female',
	OTHER = 'other'
}

export enum Sort {
	ASC = 1,
	DESC = -1
}
